protoc --go_out=. --go-grpc_out=. hello.proto

- install protoc compiler

    - install it from internet and set the path

- install generator 
```
go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
```

- protocol buffer: language agnostic transportation format like json
- 

